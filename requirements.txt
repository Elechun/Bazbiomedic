# 서버(GPU) 기준. CPU 만 있으면 torch 는 https://pytorch.org/get-started/locally/ 안내대로 설치.
torch>=2.2
torchvision>=0.17
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pillow>=10.0
matplotlib>=3.7
pytest>=7.0
# 부록 jetlab 용
opencv-python-headless>=4.8
